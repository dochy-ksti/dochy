{
  // MList is a mutable list in Dochy.
  // It's similar to CList, except it's mutable.

  mlist : ["MList",[{
    name : "a",
    val : 3,
  }],
    {
      name : "b",
      val : 4,
    },
    {
      //name is omitted
      val : 5,
    }
  ],
}

// Explanation:
//
